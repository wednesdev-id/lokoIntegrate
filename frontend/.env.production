# Production API Configuration
# If your backend is on a different domain, uncomment and set the full URL
# VITE_API_URL=https://api.loko.wednesdev.id/api

# By default, uses relative path '/api' which assumes frontend and backend are on same domain
